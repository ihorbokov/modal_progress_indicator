name: modal_progress_indicator
description: Configurable modal progress indicator that blocks access to a wrapped widget and its subtree.
version: 1.0.0
homepage: https://github.com/phimary/modal_progress_indicator

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^4.0.0
